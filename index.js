/**
 * RapidAid - Application Entry Point
 * Registers the root component with Expo
 */

import { registerRootComponent } from 'expo';
import App from './App';

// Register the main app component
registerRootComponent(App);
